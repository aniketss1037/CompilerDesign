#include <iostream>
#include <regex>

using namespace std;

int main()
{

    regex reg("a*ba*");
    if (regex_search("aabaa", reg))
        cout << "matched" << endl;
    else
        cout << "not matched" << endl;

    return 0;
}

